name: continuous_integration_build
on:
  push:
    branches: ["core"]
  pull_request:
    branches: ["core"]
jobs:
  continuous_integration_build:
    runs-on: macos-latest
    steps:
      - name: continuous_integration:repository->checkout
        uses: actions/checkout@v4
      - name: continuous_integration:make
        run: make all
